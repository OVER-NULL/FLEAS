#!/bin/bash 

# Make sure permissions are set to run as executable 
# chmod +x filename.sh (Use to change to elf if you don't know how to set file permissions) 

# Then type ./run.sh into the same directory as all other files and run 

make -f ascii.mk 
